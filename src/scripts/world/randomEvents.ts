// import GameStore from "../../store/GameStore";
// import { randomNumber } from "../../utils/utils";

export const checkForRandomEvent = () => {
    // const { addLogMessage } = GameStore;
    // const randomEvent = randomNumber(40, 0);
    // if (randomEvent === 15) addLogMessage("You hear birds chirping.");
    // if (randomEvent === 16) addLogMessage("You hear distant noise.");
    // if (randomEvent === 17) addLogMessage("You feel gentle wind blowing.");
    // if (randomEvent === 18)
    //     addLogMessage("You see somebody walking in the distance.");
    // if (randomEvent === 19) addLogMessage("You see bird flying nearby.");
    // if (randomEvent === 20) addLogMessage("You see a squirrel nearby.");
};
